{% extends 'base.html.twig' %}

{% block body %}
    <h1>Composicaoquimico</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ composicaoquimico.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('composicaoquimico_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('composicaoquimico_edit', { 'id': composicaoquimico.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
