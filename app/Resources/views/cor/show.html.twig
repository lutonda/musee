{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cor</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cor.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ cor.nome }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cor_edit', { 'id': cor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
