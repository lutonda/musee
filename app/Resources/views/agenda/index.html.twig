{% extends 'base.html.twig' %}

{% block body %}
    <h1>Agendas list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Categoria</th>
                <th>Hora</th>
                <th>Dataagenda</th>
                <th>Data</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for agenda in agendas %}
            <tr>
                <td><a href="{{ path('agenda_show', { 'id': agenda.id }) }}">{{ agenda.id }}</a></td>
                <td>{{ agenda.categoria }}</td>
                <td>{{ agenda.hora }}</td>
                <td>{% if agenda.dataagenda %}{{ agenda.dataagenda|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if agenda.data %}{{ agenda.data|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('agenda_show', { 'id': agenda.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('agenda_edit', { 'id': agenda.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('agenda_new') }}">Create a new agenda</a>
        </li>
    </ul>
{% endblock %}
