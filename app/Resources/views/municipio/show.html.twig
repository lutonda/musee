{% extends 'base.html.twig' %}

{% block body %}
    <h1>Municipio</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ municipio.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ municipio.nome }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('municipio_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
