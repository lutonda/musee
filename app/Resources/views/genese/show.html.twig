{% extends 'base.html.twig' %}

{% block body %}
    <h1>Genese</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genese.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ genese.nome }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ genese.descricao }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('genese_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('genese_edit', { 'id': genese.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
